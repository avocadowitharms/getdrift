{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-msedge",
      "request": "launch",
      "name": "Open Drift Blog",
      "url": "http://localhost:5500/blog/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Open Drift Blog Preview"
    }
  ]
}
